#lang info

(define collection 'multi)

(define deps '("srfi-lite-lib"
               "base"))

(define build-deps '("eli-tester" "racket-doc" "scribble-lib" "srfi-doc"
                     "web-server-doc"))
